NODE_ENV=production
PORT=3000
JWT_SECRET=change-this-to-a-secure-random-string-in-production
ADMIN_PASSWORD_HASH=change-this-to-bcrypt-hash-of-admin-password
DATA_DIR=/app/data
UPLOADS_DIR=/app/uploads
SLIDES_DIR=/app/slides
ASSETS_DIR=/app/assets

# OnlyOffice DocumentServer
# Optional JWT secret for OnlyOffice (leave empty to disable JWT)
ONLYOFFICE_JWT_SECRET=
# Backend URL for OnlyOffice to access files (usually http://webinar-backend:3000)
# This is used by OnlyOffice to download files for conversion
BACKEND_URL=http://webinar-backend:3000

# User/Group IDs for container processes
PUID=1000
PGID=1000
